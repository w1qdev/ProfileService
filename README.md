# ProfileService

Полнофункциональное приложение для управления профилями пользователей с аутентификацией и авторизацией. Проект состоит из backend-сервера на Express.js

## 📋 Описание проекта

**ProfileService** - это система управления пользователями с поддержкой:

- Регистрации и аутентификации пользователей
- JWT-токенов (access и refresh)
- Управления ролями (USER, ADMIN)
- Блокировки пользователей
- Управления профилями

### Технологический стек

**Backend:**

- Node.js + Express.js
- TypeScript
- PostgreSQL + Prisma ORM
- JWT для аутентификации
- bcrypt для хеширования паролей
- Zod для валидации данных

## 🚀 Быстрый старт

### Предварительные требования

- Node.js (v18+)
- npm или yarn
- PostgreSQL (локально или Docker)

### Установка и запуск

#### 1. Клонирование репозитория

```bash
git clone https://github.com/w1qdev/ProfileService
cd ProfileService
```

#### 2. Настройка переменных окружения

Создайте файл `.env` в корневой директории проекта на основе `.env.example`:

```bash
cp .env.example .env
```

Отредактируйте `.env` с вашими параметрами:

```env
# Server
NODE_ENV=development
PORT=3000
HOST=localhost

# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/test_app

# JWT
JWT_ACCESS_SECRET=your_jwt_secret_access_key
JWT_REFRESH_SECRET=your_jwt_secret_refresh_key
JWT_ACCESS_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=3m

# BCRYPT
BCRYPT_SECRET=your_bcrypt_secret_key
```

#### 3. Установка зависимостей

**Backend:**

```bash
npm install
```

**Frontend:**

```bash
cd client
npm install
cd ..
```

#### 4. Настройка базы данных

Убедитесь, что PostgreSQL запущен и доступен по адресу из `DATABASE_URL`.

Выполните миграции:

```bash
npm run prisma:migrate
```

Сгенерируйте Prisma клиент:

```bash
npm run prisma:generate
```

#### 5. Запуск проекта

**Вариант 1: Запуск в режиме разработки (рекомендуется)**

Откройте два терминала:

Терминал 1 - Backend:

```bash
npm run dev
```

Сервер запустится на `http://localhost:3000`

Терминал 2 - Frontend:

```bash
cd client
npm run dev
```

Клиент запустится на `http://localhost:5173`

**Вариант 2: Production сборка**

Backend:

```bash
npm run build
npm start
```

Frontend:

```bash
cd client
npm run build
npm run preview
```

## 📚 API Endpoints

| Метод | Endpoint                     | Доступ     | Описание                           |
| ----- | ---------------------------- | ---------- | ---------------------------------- |
| POST  | `/api/auth/register`         | Все        | Регистрация нового пользователя    |
| POST  | `/api/auth/login`            | Все        | Вход в систему                     |
| POST  | `/api/auth/refresh-token`    | Все        | Обновить access token              |
| GET   | `/api/users/:id`             | User/Admin | Получить профиль пользователя      |
| GET   | `/api/users`                 | Admin      | Получить список всех пользователей |
| POST  | `/api/users/:id/block`       | User/Admin | Заблокировать пользователя         |
| POST  | `/api/users/:id/grant-admin` | Admin      | Выдать права администратора        |

## 🧪 Тестирование API

Проект включает файл `request.http` для удобного тестирования всех API эндпоинтов.

### Использование с VS Code

1. Установите расширение [REST Client](https://marketplace.visualstudio.com/items?itemName=humao.rest-client)
2. Откройте файл `request.http`
3. Нажмите на "Send Request" над каждым запросом или используйте `Ctrl+Alt+R`

### Использование с других IDE

Вы можете скопировать запросы из `request.http` в:

- **Postman** - импортируйте как raw текст

### Рекомендуемый порядок тестирования

1. **Регистрация** - создайте тестовых пользователей
2. **Вход** - получите access и refresh токены
3. **Получение профиля** - используйте access token
4. **Получение всех пользователей** - требует admin роли
5. **Блокировка пользователя** - заблокируйте пользователя
6. **Выдача admin роли** - требует admin роли

### Важные замечания

- Замените placeholder токены на реальные значения из ответов
- Замените ID пользователей на реальные ID из ответов
- Все защищенные эндпоинты требуют валидный access token в заголовке `Authorization: Bearer <token>`
- Refresh token используется для получения нового access token

## 🗄️ Структура проекта

```
ProfileService/
├── src/                          # Backend код
│   ├── api/
│   │   ├── controllers/          # Обработчики запросов
│   │   └── routes/               # API маршруты
│   ├── services/                 # Бизнес-логика
│   ├── middlewares/              # Express middleware
│   ├── shared/                   # Общие утилиты
│   ├── database/                 # Конфигурация БД
│   └── server.ts                 # Точка входа сервера
├── prisma/
│   ├── schema.prisma             # Схема БД
│   └── migrations/               # Миграции БД
├── .env.example                  # Пример переменных окружения
├── package.json                  # Зависимости backend
└── README.md                     # Этот файл
```

## 🔧 Доступные команды

### Backend

```bash
npm run dev              # Запуск в режиме разработки с hot-reload
npm run build            # Компиляция TypeScript
npm start                # Запуск скомпилированного приложения
npm run prisma:generate  # Генерация Prisma клиента
npm run prisma:migrate   # Запуск миграций БД
npm run prisma:studio    # Открыть Prisma Studio (UI для БД)
```

## 🔐 Аутентификация

Проект использует JWT-токены для аутентификации:

- **Access Token** - короткоживущий токен (по умолчанию 15 минут)
- **Refresh Token** - долгоживущий токен для обновления access token

При логине клиент получает оба токена. Access token передается в заголовке `Authorization: Bearer <token>` для защищенных запросов.

## 📝 Модель данных

### User

```prisma
model User {
  id           String    @id @default(uuid())
  fullName     String
  email        String    @unique
  birthDate    String
  password     String    (хеширован bcrypt)
  refreshToken String?
  role         UserRole  @default(USER)      # USER или ADMIN
  status       UserStatus @default(ACTIVE)   # ACTIVE или BLOCKED
  createdAt    DateTime  @default(now())
  updatedAt    DateTime  @updatedAt
}
```
